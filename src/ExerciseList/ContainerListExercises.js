function ContainerListExercises({children}){ // En react cuando escribimos una funcion(componente) en lo que antes era parametros, ahora en react se llaman props y son las cualidades que posee la funcion para despues ejecutarlas con los elementos del componente
    return(
        children
    )
}
export { ContainerListExercises }